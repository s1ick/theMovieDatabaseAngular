# 🎬 Movie Finder — Angular Edition

[![Angular](https://img.shields.io/badge/Angular-20-DD0031?logo=angular&logoColor=white)](https://angular.dev/)
[![Firebase](https://img.shields.io/badge/Firebase-Authentication-orange?logo=firebase)](https://firebase.google.com/)
[![TailwindCSS](https://img.shields.io/badge/TailwindCSS-3.4-38BDF8?logo=tailwindcss&logoColor=white)](https://tailwindcss.com/)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.0-blue?logo=typescript&logoColor=white)](https://www.typescriptlang.org/)

> Тестовое задание: каталог фильмов на **Angular** с поиском, избранным и авторизацией.
> **https://github.com/s1ick/theMovieDatabaseAngular**

---

## 🚀 Быстрый запуск

```bash
# Установка зависимостей
npm install

# Запуск проекта
npm start
```

После запуска приложение откроется по адресу:  
👉 **http://localhost:4200**

---

## 🔐 Тестовый аккаунт

**Логин:** `admin@admin.ru`  
**Пароль:** `123456`

---

## 📱 Основные функции

- 🔍 Поиск фильмов через **OMDb API**  
- ❤️ Добавление в **избранное** с сохранением в **Firebase**  
- 👤 Авторизация (**Email / Google**)  
- 📱 Полностью **адаптивный дизайн**  
- 🖼 Заглушки для **битых постеров**  
- 📥 **Экспорт избранного в CSV**

---

## 🛠 Используемые технологии

| Технология | Назначение |
|-------------|------------|
| **Angular 20** | Фреймворк фронтенда |
| **Firebase** | Аутентификация и база данных |
| **Tailwind CSS** | Современные стили |
| **TypeScript** | Строгая типизация |
| **RxJS** | Реактивные и асинхронные операции |

---

## 📁 Структура проекта

```
src/
├── components/     # Компоненты интерфейса
├── services/       # Бизнес-логика и работа с API
└── environments/   # Настройки и ключи
```

---

## 🔑 API Ключи

В проекте используются реальные ключи для интеграций:

- **Firebase** — аутентификация и хранение данных  
- **OMDb API** — поиск фильмов по названию

---
# 🧠 Ответы на тестовые вопросы — Frontend Developer (Junior)

---

## 🪄 1. Распространение событий в JavaScript (Event Propagation)

Когда в браузере происходит событие (например, клик по кнопке), оно не останавливается только на этом элементе.  
Оно проходит через дерево элементов — **сверху вниз**, а потом **обратно вверх**.  
Этот процесс называется **распространением событий** (*Event Propagation*).

### 🔹 Фазы распространения:
1. **Capturing (захват)** — событие идёт сверху вниз, от `document` до элемента, по которому кликнули.  
2. **Target (цель)** — событие достигает самого элемента, где произошло действие.  
3. **Bubbling (всплытие)** — событие поднимается обратно вверх по дереву элементов.

### 💡 Зачем это нужно на практике:
- **Делегирование событий** — можно повесить один обработчик на родителя и ловить клики от дочерних элементов (например, элементов списка).  
- **Контроль поведения** — можно остановить всплытие с помощью `event.stopPropagation()`.  
- **Ранняя обработка** — использование `capture: true`, чтобы отреагировать ещё на фазе захвата.

---

## ⚙️ 2. Promise и асинхронный код

**Promise** — это объект, который помогает работать с асинхронным кодом  
(например, с запросами на сервер). Он сообщает: «результат появится позже».

### 🔹 Состояния Promise:
- `pending` — выполняется;
- `fulfilled` — успешно завершён;
- `rejected` — произошла ошибка.

Когда Promise завершается, результат обрабатывается с помощью:
```js
promise
  .then(result => console.log(result))
  .catch(error => console.error(error))
  .finally(() => console.log('Done'));
```

### 🧩 Другие способы работы с асинхронностью:
- **`async/await`** — делает код читаемым и похожим на синхронный:
  ```js
  async function fetchData() {
    try {
      const data = await fetch('/api/data');
      console.log(await data.json());
    } catch (e) {
      console.error(e);
    }
  }
  ```
- **Callbacks (колбэки)** — старый способ: функция вызывается после завершения операции.

### 🔁 Event Loop
**Event Loop (событийный цикл)** управляет очередью задач,  
позволяя JavaScript — несмотря на однопоточность — работать асинхронно.

---

## 🧱 3. Объектно-ориентированное программирование (ООП) в JavaScript

**ООП** — это подход, в котором код строится вокруг **объектов**,  
объединяющих **данные** и **методы**, которые с ними работают.

### 🔹 Принципы ООП:
- **Инкапсуляция** — скрываем детали реализации, оставляя только нужные методы.  
- **Наследование** — классы могут расширять другие, избегая дублирования кода.  
- **Полиморфизм** — одинаковые методы могут вести себя по-разному в разных классах.  
- **Абстракция** — показываем только суть, без лишних деталей.

### 💻 Пример:
```js
class Person {
  constructor(name) {
    this.name = name;
  }

  greet() {
    console.log(`Hi, ${this.name}`);
  }
}

const user = new Person('Ivan');
user.greet(); // Hi, Ivan
```

> Современный синтаксис `class` — это удобная надстройка над прототипами,  
> на которых исторически построен JavaScript.

---

## 🌐 4. Что происходит, когда мы вводим URL в браузере

### 🔹 Этапы:
1. **Ввод URL** — браузер разбирает адрес (протокол, домен, путь).  
2. **DNS-запрос** — получает IP-адрес сервера.  
3. **Установка соединения** — создаётся соединение через TCP/HTTPS.  
4. **Отправка запроса** — браузер отправляет HTTP-запрос на сервер.  
5. **Ответ сервера** — приходит HTML, CSS, JS и другие данные.  
6. **Парсинг и рендеринг** — создаются DOM и CSSOM, страница отображается на экране.

### ⚡ Как ускорить загрузку:
- Использовать **кэширование**, **CDN**, **минификацию**, **lazy-loading**;  
- Переходить на **HTTP/2** и **HTTP/3**.

### 🧱 Возможные проблемы и защита:
- **CORS** — ограничение запросов к другим доменам;  
- **XSS** — защита от внедрения вредоносных скриптов;  
- **CSRF** — защита от поддельных запросов от имени пользователя.

> Эти механизмы делают интернет одновременно **быстрым** и **безопасным**.

## 👨‍💻 Автор
Ivan Matveev
